# Selecionar variáveis de confiança institucional
dados_conf <- dados_final %>%
select(trstprl, trstlgl, trstplc, trstplt, trstprt, trstep, trstun) %>%
drop_na()
# Selecionar variáveis de confiança institucional
dados_conf <- dados_final %>%
select(trstprl, trstlgl, trstplc, trstplt, trstprt, trstep, trstun) %>%
drop_na()
library(tidyverse)
# Selecionar variáveis de confiança institucional
dados_conf <- dados_final %>%
select(trstprl, trstlgl, trstplc, trstplt, trstprt, trstep, trstun) %>%
drop_na()
# Selecionar variáveis de confiança institucional
dados_conf <- dados_final %>%
select(trstprl, trstlgl, trstplc, trstplt, trstprt, trstep, trstun) %>%
drop_na()
# Pacotes necessários
library(dplyr)
library(psych)
library(corrplot)
library(factoextra)
# 1. Selecionar variáveis de confiança institucional (casos completos)
dados_conf <- dados_final %>%
select(trstprl, trstlgl, trstplc, trstplt, trstprt, trstep, trstun) %>%
drop_na()
# Selecionar variáveis de confiança institucional
dados_conf <- dados_final %>%
select(trstprl, trstlgl, trstplc, trstplt, trstprt, trstep, trstun) %>%
drop_na()
